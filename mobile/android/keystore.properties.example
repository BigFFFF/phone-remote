# Copy to keystore.properties only when release signing is configured.
# Never commit the real file or the keystore.
storeFile=C:\\secure\\path\\phone-remote-release.jks
storePassword=replace-with-secret
keyAlias=phone-remote
keyPassword=replace-with-secret
